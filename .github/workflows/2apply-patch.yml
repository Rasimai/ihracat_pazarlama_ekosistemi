diff --git a/.github/workflows/00-smoke-runner.yml b/.github/workflows/00-smoke-runner.yml
new file mode 100644
index 0000000..7b1c0af
--- /dev/null
+++ b/.github/workflows/00-smoke-runner.yml
@@ -0,0 +1,34 @@
+name: Runner Smoke (local)
+on:
+  workflow_dispatch:
+jobs:
+  info:
+    runs-on: [self-hosted, local]
+    permissions: {}
+    steps:
+      - uses: actions/checkout@v4
+      - name: System info
+        run: |
+          echo "User: $(whoami)"
+          echo "Hostname: $(hostname)"
+          echo "Uptime: $(uptime)"
+          echo "Kernel/uname:"
+          uname -a
+          echo "macOS sürümü (varsa):"
+          sw_vers 2>/dev/null || true
+          echo "CPU:"
+          sysctl -n machdep.cpu.brand_string 2>/dev/null || true
+      - name: Git status
+        run: |
+          git status || true
+      - name: Docker durum
+        run: |
+          command -v docker >/dev/null && docker ps || echo "Docker bulunamadı (normal olabilir)."
+
diff --git a/.github/workflows/ops-shell.yml b/.github/workflows/ops-shell.yml
new file mode 100644
index 0000000..ab013cd
--- /dev/null
+++ b/.github/workflows/ops-shell.yml
@@ -0,0 +1,65 @@
+name: Ops Shell (limited)
+on:
+  workflow_dispatch:
+    inputs:
+      command:
+        description: "Çalıştırılacak komut (whitelist)"
+        required: true
+        type: choice
+        options:
+          - uptime
+          - df
+          - list
+          - processes
+          - docker-ps
+permissions: {}
+jobs:
+  exec:
+    runs-on: [self-hosted, local]
+    steps:
+      - name: Komutu çalıştır
+        id: run
+        shell: bash
+        run: |
+          set -euo pipefail
+          cmd="${{ github.event.inputs.command }}"
+          case "$cmd" in
+            uptime)
+              echo ">>> uptime"; uptime
+              ;;
+            df)
+              echo ">>> df -h"; df -h
+              ;;
+            list)
+              echo ">>> ls -la (home)"; ls -la "$HOME"
+              ;;
+            processes)
+              echo ">>> ps aux | head -n 30"; ps aux | head -n 30
+              ;;
+            docker-ps)
+              echo ">>> docker ps"; docker ps
+              ;;
+            *)
+              echo "Komut whitelist'te değil."; exit 2
+              ;;
+          esac | tee ops-output.txt
+      - name: Çıktıyı artefact olarak yükle
+        uses: actions/upload-artifact@v4
+        with:
+          name: ops-output
+          path: ops-output.txt
